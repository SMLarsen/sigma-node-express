// function to validate for empty fields

function valEmptyFields(newSong) {
  if (newSong.title === "" || newSong.artist === "") {
    return false;
  } else {
    return false;
  }
}

module.exports = valEmptyFields;
